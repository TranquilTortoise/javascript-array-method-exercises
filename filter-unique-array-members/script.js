// take an array, and return a new array with only one copy of each element from the original

function unique(arr) {

    let uniqueArr = [];

    for (let str of arr) {

        if (!result.includes(str)) {

            result.push(str);
        }
    }
    return uniqueArr;
}